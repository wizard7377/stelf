; FIXME: (private_modules S) was removed so that Pal.ml can apply Make_Recon
; with an anonymous struct argument — OCaml cannot check conformance against a
; private module type.  Restore once Pal.ml supplies a named module satisfying S.S.

(library
 (instrumentation
  (backend bisect_ppx))
 (name recon)
 (public_name stelf.recon)
 (libraries
  display
  cst
  syntax
  modules
  thm
  modes
  names
  intsyn
  print
  typecheck
  timing
  msg
  global
  table
  basis
  debug
  solvers)
 (flags
  (:standard -w -A+5+66))
 (preprocess
  (pps ppx_deriving.show ppx_deriving.eq ppx_deriving.ord)))
