; (include sources.dune)

(library
 (name heuristic)
 (public_name twelf.heuristic)
 (modules heuristic_ heuristic_sum)
 (libraries basis)
 (flags
  (:standard -open Basis))
 (wrapped true)
(preprocess (staged_pps ppx_import ppx_deriving.show ppx_deriving.eq ppx_deriving.ord)))
