
      (rule
 (mode fallback)
 (target worldSyn.sig.ml)
 (action
  (write-file worldSyn.sig.ml "")))

(rule
 (mode fallback)
 (target worldSyn.fun.ml)
 (action
  (write-file worldSyn.fun.ml "")))

(rule
 (mode fallback)
 (target worldSyn.sml.ml)
 (action
  (write-file worldSyn.sml.ml "")))

(rule
 (target worldSyn.ml)
 (deps
  (:sig worldSyn.sig.ml)
  (:fun worldSyn.fun.ml)
  (:sml worldSyn.sml.ml))
 (action
  (progn
   (concurrent
    (copy# %{sig} %{sig}s)
    (copy# %{fun} %{fun}s)
    (copy# %{sml} %{sml}s))
   (with-stdout-to
    %{target}
    (progn
     (cat %{sig}s)
     (echo "\n")
     (cat %{fun}s)
     (echo "\n")
     (cat %{sml}s))))))
      
      (rule
 (mode fallback)
 (target worldify.sig.ml)
 (action
  (write-file worldify.sig.ml "")))

(rule
 (mode fallback)
 (target worldify.fun.ml)
 (action
  (write-file worldify.fun.ml "")))

(rule
 (mode fallback)
 (target worldify.sml.ml)
 (action
  (write-file worldify.sml.ml "")))

(rule
 (target worldify.ml)
 (deps
  (:sig worldify.sig.ml)
  (:fun worldify.fun.ml)
  (:sml worldify.sml.ml))
 (action
  (progn
   (concurrent
    (copy# %{sig} %{sig}s)
    (copy# %{fun} %{fun}s)
    (copy# %{sml} %{sml}s))
   (with-stdout-to
    %{target}
    (progn
     (cat %{sig}s)
     (echo "\n")
     (cat %{fun}s)
     (echo "\n")
     (cat %{sml}s))))))
      
      (rule
 (mode fallback)
 (target worldcheck_.sig.ml)
 (action
  (write-file worldcheck_.sig.ml "")))

(rule
 (mode fallback)
 (target worldcheck_.fun.ml)
 (action
  (write-file worldcheck_.fun.ml "")))

(rule
 (mode fallback)
 (target worldcheck_.sml.ml)
 (action
  (write-file worldcheck_.sml.ml "")))

(rule
 (target worldcheck_.ml)
 (deps
  (:sig worldcheck_.sig.ml)
  (:fun worldcheck_.fun.ml)
  (:sml worldcheck_.sml.ml))
 (action
  (progn
   (concurrent
    (copy# %{sig} %{sig}s)
    (copy# %{fun} %{fun}s)
    (copy# %{sml} %{sml}s))
   (with-stdout-to
    %{target}
    (progn
     (cat %{sig}s)
     (echo "\n")
     (cat %{fun}s)
     (echo "\n")
     (cat %{sml}s))))))
      