; (include sources.dune)

(library
 (instrumentation
  (backend bisect_ppx))
 (name terminate)
 (public_name stelf.terminate)
 (modules Checking Reduces Terminate_ CHECKING REDUCES)
 (libraries
  display
  global
  trail
  intsyn
  names
  print
  formatter
  index
  subordinate
  paths
  solvers
  basis)
 (flags
  (:standard -w -A+5))
 (wrapped true)
 (preprocess
  (pps ppx_deriving.show ppx_deriving.eq ppx_deriving.ord)))
