(include sources.dune)

(library
 (name lambda)
 (public_name twelf.lambda)
 (modules
  fgnopn
  fgnopntable
  order
  intsyn
  whnf
  conv
  tomega
  constraints
  unify
  match
  abstract
  approx
  lambda_)
 (libraries global trail table basis)
 (flags
  (:standard -w -A -open Basis -open Global_ -open Trail_ -open Table_))
 (wrapped false))
