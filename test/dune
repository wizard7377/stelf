; (include sources.dune)

(test
 (name regression)
 (libraries twelf alcotest basis regression_cases)
 (deps
  (source_tree ../examples)
  (source_tree ../examples_clp))
 (action
  (chdir
   %{workspace_root}/test
   (run %{exe:regression.exe})))
 (flags
  (:standard -open Basis)))
