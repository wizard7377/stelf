# Booleans

%sort bool %.
%scope bool %{
  %term true bool
  %term false bool
%}

%sort not bool bool %.
%scope not %{
  %term t not %(true bool) %(false bool)
  %term f not %(false bool) %(true bool)
%}

%mode not %in %out %.
%worlds () (not _ _)
%total X (not X _)
